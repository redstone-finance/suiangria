name: Ts CI

on:
  workflow_dispatch:
  pull_request:
    types: [synchronize, opened]
  push:
    branches:
      - main

jobs:
  test-and-lint:
    runs-on: macos-latest

    strategy:
      matrix:
        node-version: [22, 24]

    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: ${{ matrix.node-version }}
          cache: yarn

      - name: Enable corepack
        run: corepack enable

      - name: Install dependencies
        run: yarn install --immutable

      - name: TypeScript/JavaScript lint
        run: yarn lint

      - name: Prettier format check
        run: yarn format:prettier --check

      - name: TOML format check
        run: yarn format:toml --check

      - name: TypeScript build
        run: yarn build:ts

      - name: Install sui cli
        run: |
          curl -sSfL -H'Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}' https://raw.githubusercontent.com/Mystenlabs/suiup/main/install.sh | sh
          suiup install sui@testnet-1.56.1
          suiup default set sui@testnet-1.56.1
          sui client -y

      - name: Run tests
        run: yarn build:test
